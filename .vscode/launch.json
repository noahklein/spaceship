{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "(gdb) Launch",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/src.bin",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "build",
        }
    ]
}